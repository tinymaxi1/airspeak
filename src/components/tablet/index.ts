/**
 * Tablet support — Phase 13
 *
 * Usage:
 *   import { TabletShell } from '@/components/tablet';
 *   <TabletShell><YourScreen /></TabletShell>
 *
 * Phone: pass-through (no overhead)
 * Tablet (≥768px): centered 560px max-width + side decoration
 */
export { TabletShell } from './TabletShell';
